# Оптимизатор волн миграции ПО на Linux

Streamlit приложение для оптимизации планирования миграции рабочих станций с Windows на Linux.

## Описание

Приложение решает задачу максимального покрытия (Maximum Coverage Problem): определяет оптимальный набор ПО для тестирования в каждой волне миграции, чтобы максимизировать количество рабочих станций, которые могут быть полностью переведены на Linux.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

Запустите приложение командой:
```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу http://localhost:8501

## Использование

1. **Загрузите данные**: Загрузите Excel или CSV файл с информацией об установленном ПО
2. **Выберите столбцы**: Укажите столбец с идентификаторами устройств и столбец с наименованиями ПО
3. **Обработайте данные**: Нажмите кнопку "Обработать данные"
4. **Выберите режим**:
   - **Ручной режим**: Задайте количество волн и лимиты ПО для каждой волны
   - **Автоматический режим**: Получите рекомендации системы
5. **Экспортируйте результаты**: Сохраните план миграции в Excel файл

## Формат входных данных

Файл должен содержать минимум два столбца:
- Идентификатор устройства/пользователя (например, "Устройство.Сетевое Имя устройства (уст-во, хост)")
- Наименование ПО (например, "Программное обеспечение")

## Выходные данные

Приложение генерирует Excel файл `migration_plan_result.xlsx` с тремя листами:

1. **Data**: Исходные данные + столбец "Волна миграции"
2. **Статистика по волнам**: Таблица с результатами по каждой волне
3. **Общая статистика**: Общая статистика и метрики покрытия

## 📊 Расчёт статистики

### Основные метрики

#### **Всего АРМ/пользователей**
Количество уникальных рабочих станций в исходном файле (после удаления дубликатов из столбца с устройствами).

**Пример:** 3,590 устройств

#### **Всего уникального ПО**
Количество различных программ во всём файле (уникальные значения из столбца с ПО).

**Пример:** 1,349 программ

#### **Уникальных наборов ПО**
Количество различных **комбинаций** ПО на устройствах. Если у двух АРМ идентичный список программ - это один набор.

**Формула:**
```
Для каждого АРМ: собрать набор всех его программ
Подсчитать количество уникальных наборов
```

**Пример:** 3,189 наборов из 3,590 АРМ (многие устройства имеют схожую конфигурацию)

#### **Среднее ПО на АРМ**
Среднее количество программ на одном устройстве.

**Формула:**
```
(Общее количество строк в файле) / (Количество уникальных АРМ)
```

**Пример:** 112,017 строк / 3,590 АРМ = 31.2 программы/АРМ

### Метрики после расчёта волн

#### **Всего АРМ, покрытых планом**
Количество устройств, которые **смогут мигрировать** (все их программы включены в план тестирования).

**Критерий:** АРМ покрыт ⟺ Все его программы протестированы

**Пример:**
```
План: {Word, Excel, Chrome, Firefox}

АРМ-1: {Word, Excel}        → ПОКРЫТ ✓
АРМ-2: {Word, Excel, Skype} → НЕ ПОКРЫТ ✗ (Skype не в плане)
АРМ-3: {Chrome}             → ПОКРЫТ ✓
```

#### **Всего ПО, включённого в план**
Количество программ, выбранных для тестирования во всех волнах (объединение программ из всех волн).

#### **Процент покрытия АРМ**
Доля устройств, которые смогут мигрировать.

**Формула:**
```
(АРМ покрытых / Всего АРМ) × 100%
```

**Пример:** 3,420 / 3,590 × 100% = **95.2%**

**Интерпретация:**
- **>90%** - отличный результат, можно запускать миграцию
- **70-90%** - хороший результат, можно добавить ещё волну
- **<70%** - низкое покрытие, нужно увеличить лимиты

#### **Процент использования ПО**
Доля всего уникального ПО, попавшая в план.

**Формула:**
```
(ПО в плане / Всего уникального ПО) × 100%
```

**Пример:** 680 / 1,349 × 100% = **50.4%**

**Интерпретация:**
- **40-60%** - оптимально (баланс эффективности и покрытия)
- **>70%** - тестируется много редкого ПО (возможно, неэффективно)
- **<40%** - очень эффективно (малые усилия, большое покрытие)

### Эффективность оптимизации

**Закон Парето в действии:**
```
50% ПО (680 программ) → 95% АРМ (3,420 устройств)
```

Это показывает, что оптимизация работает: протестировав половину ПО, можно мигрировать почти все устройства.

**Сравнение подходов:**

| Подход | ПО в плане | Покрыто АРМ | Эффективность |
|--------|-----------|-------------|---------------|
| Наивный (самые популярные) | 100 | ~800 (22%) | Низкая |
| Оптимизация (Maximum Coverage) | 100 | ~1,437 (40%) | Высокая |

**Выигрыш:** почти в **2 раза** больше устройств при тех же затратах!

### Метрики по волнам

**АРМ мигрировано в волне** - устройства, завершающие миграцию именно в этой волне (новые, не считая предыдущие волны).

**АРМ накопительно** - общее количество мигрировавших устройств с начала до текущей волны.

**Пример:**
```
Волна 1: 50 ПО → 1,437 АРМ (накопительно: 1,437)
Волна 2: 50 ПО → 744 АРМ   (накопительно: 2,181)
```

📖 **Подробнее см. СТАТИСТИКА.md**

## Алгоритм

Приложение использует жадный алгоритм оптимизации, который:
1. Группирует устройства по идентичным наборам ПО (ключевая оптимизация)
2. На каждой итерации выбирает ПО, которое позволяет "завершить" максимальное число устройств
3. Учитывает, что устройство может мигрировать только если ВСЁ его ПО протестировано

## Структура проекта

- `app.py` - Главный файл Streamlit приложения
- `data_processor.py` - Модуль обработки и подготовки данных
- `optimizer.py` - Модуль с алгоритмами оптимизации
- `requirements.txt` - Зависимости проекта
- `test_modules.py` - Тестирование модулей с реальными данными
- `run.bat` - Скрипт запуска для Windows
- `README.md` - Техническая документация (этот файл)
- `ИНСТРУКЦИЯ.md` - Подробная инструкция пользователя на русском
- `СТАТИСТИКА.md` - Детальное объяснение расчёта всех метрик
- `CLAUDE.md` - Документация для Claude Code
- `методика.md` - Техническое задание и описание алгоритма

## Технические детали

Решаемая задача относится к классу NP-трудных задач (Maximum Coverage Problem / Set Cover). Используется жадный аппроксимирующий алгоритм, который обеспечивает хорошие результаты за приемлемое время.

Ключевая оптимизация: вместо анализа ~1600 отдельных устройств, приложение группирует их по уникальным наборам ПО, что значительно сокращает пространство поиска.
